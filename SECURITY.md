# SECURITY.md

## 1. 安全策略

本项目遵循最小暴露原则：

- API Key 仅从环境变量或 `.env` 读取
- 禁止在代码与日志中明文输出密钥
- 输出路径强制限制在项目目录内，防止路径穿越

## 2. 密钥管理

- 使用 `.env` 本地存储密钥，`.env` 已在 `.gitignore` 中忽略
- 不要在 issue、PR、日志中提交真实密钥

## 3. 日志与隐私

- 日志过滤器会掩码 `MOONSHOT_API_KEY`
- 如需共享日志，请二次人工脱敏

## 4. 依赖安全

- 建议定期执行依赖升级
- 建议在 CI 增加依赖漏洞扫描（如 `pip-audit`）

## 5. 漏洞披露

发现安全问题请私下联系维护者，不要公开披露可利用细节。
